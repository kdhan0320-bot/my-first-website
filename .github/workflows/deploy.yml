name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Install dependencies (mini_sns)
        working-directory: projects/mini_sns
        run: npm ci

      - name: Build mini_sns
        working-directory: projects/mini_sns
        env:
          NODE_ENV: production
        run: npm run build

      - name: Install dependencies (my-portfolio)
        working-directory: projects/my-portfolio
        run: npm ci

      - name: Build my-portfolio
        working-directory: projects/my-portfolio
        run: npm run build

      - name: Install dependencies (my-community)
        working-directory: projects/my-community
        run: npm ci

      - name: Build my-community
        working-directory: projects/my-community
        env:
          VITE_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: npm run build

      - name: Install dependencies (jobflow-dashboard)
        working-directory: projects/jobflow-dashboard
        run: npm ci

      - name: Build jobflow-dashboard
        working-directory: projects/jobflow-dashboard
        env:
          VITE_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          NODE_ENV: production
        run: npm run build

      - name: Prepare site directory
        run: |
          mkdir -p _site
          cp index.html _site/index.html
          cp -r projects/mini_sns/dist/. _site/mini-sns
          cp _site/mini-sns/index.html _site/mini-sns/404.html
          cp -r projects/my-portfolio/dist/. _site/my-portfolio
          cp _site/my-portfolio/index.html _site/my-portfolio/404.html
          cp -r projects/my-community/dist/. _site/my-community
          cp _site/my-community/index.html _site/my-community/404.html
          cp -r projects/jobflow-dashboard/dist/. _site/jobflow-dashboard
          cp _site/jobflow-dashboard/index.html _site/jobflow-dashboard/404.html
          mkdir -p _site/ott-service
          cp -r "projects/OTT Service/." _site/ott-service

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: '_site'

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
